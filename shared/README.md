# Shared

Cross-bot utilities that are not specific to crypto, gold, or QQQ.

| Module | Role |
|---|---|
| `feeds.py` | Yahoo Finance OHLCV download + CSV cache (`data_cache/`) |

Crypto market data stays in `orbit/data.py` (Binance).
